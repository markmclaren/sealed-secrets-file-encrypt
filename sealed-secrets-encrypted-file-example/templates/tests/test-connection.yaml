apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sealed-secrets-encrypted-file-example.fullname" . }}-test-connection"
  labels:
{{ include "sealed-secrets-encrypted-file-example.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sealed-secrets-encrypted-file-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
